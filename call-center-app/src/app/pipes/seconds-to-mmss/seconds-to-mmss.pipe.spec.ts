import { SecondsToMMSSPipe } from './seconds-to-mmss.pipe';

describe('SecondsToMMSSPipe', () => {
    it('create an instance', () => {
        const pipe = new SecondsToMMSSPipe();
        expect(pipe).toBeTruthy();
    });
});
